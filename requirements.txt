requests>=2.32
python-dotenv>=1.0

# Traduction locale GPU (optionnel — local uniquement, pas en CI)
# pip install ctranslate2 sentencepiece transformers torch
# pip install nvidia-cublas-cu12 nvidia-cuda-runtime-cu12
